// Archivo temporal renombrado a backup para evitar errores de compilación.
// Este archivo estaba causando un error de sintaxis (nombre de clase con punto).
// Se mantiene como backup no compilable intencionadamente.
package com.sgturnos.controller;

/* backup de UserController - contenido movido/gestionado en otra versión
	Si necesitas restaurarlo, revisa el historial del repositorio. */

// clase vacía no pública para evitar problemas en el build
class UserControllerTempBackup { }
